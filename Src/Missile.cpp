#include "Missile.h"
#include "Sprite.h"
using namespace std;

Missile::Missile (Coordinate position, Coordinate maxSpeed, Image graphic, Image hitbox, string name) : Sprite(position, maxSpeed, graphic, hitbox, name)
{
    //ctor
}
